---
description: Build and run the swap demo against mock providers, then summarize.
---

Run the full demo and report what happened.

1. `npm run build`
2. `node dist/cli.js demo`
3. Summarize:
   - which providers swapped, in what order
   - what triggered each swap (matched pattern)
   - final exit state
   - anything that looked wrong (unexpected output, errors, hangs)

Do not change any code. This is observation only.
